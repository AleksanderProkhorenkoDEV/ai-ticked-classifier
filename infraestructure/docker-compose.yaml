services:
  mysql:
    image: mysql:8.0
    container_name: ai-tickedclassify-db
    restart: unless-stopped
    volumes:
      - mysql_data:/var/lib/mysql
    ports:
      - "${MYSQL_PORT:-3307}:3306"
    env_file:
      - ../.env

volumes:
  mysql_data:
